import pandas as pd

# Create a sample DataFrame with missing values
data = {'Name': ['Alice', 'Bob', 'Alice'], 'Age': [25, None, 25]}
df = pd.DataFrame(data)

# Automated cleaning steps
df = df.drop_duplicates()               # Remove duplicate rows
df['Age'] = df['Age'].fillna(df['Age'].median()) # Fill missing with median
df.columns = df.columns.str.lower()     # Standardize column names

print(df)
import pandas as pd

# Create a sample DataFrame with missing values
data = {'Name': ['Alice', 'Bob', 'Alice'], 'Age': [25, None, 25]}
df = pd.DataFrame(data)

# Automated cleaning steps
df = df.drop_duplicates()               # Remove duplicate rows
df['Age'] = df['Age'].fillna(df['Age'].median()) # Fill missing with median
df.columns = df.columns.str.lower()     # Standardize column names

print(df)
